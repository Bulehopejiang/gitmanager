# 🧪 Git 管理器测试指南

本指南提供三层测试：**自动化自测**（一条命令跑完核心 git 操作）、**手工功能验收**（逐项对照需求）、**异常场景测试**（可靠性验证）。

---

## 一、自动化自测（最快、最省事）

```bash
python git_manager.py --selftest
```

- 在临时仓库中自动执行 15 项检查，全部 `[PASS]` 时退出码为 0：
  init / add / commit / log 解析 / 当前分支 / 二次提交 / 分支创建与切换 /
  stash 暂存与恢复 / 三种版本回溯（检出新分支、软回溯、硬回溯）/ 文件树遍历 / 无远程 push 提示
- 运行结束自动清理临时仓库，可重复执行。

**结果判断**：`自测结果：15/15 通过` + `exit=0` 即为通过；任何 `[FAIL]` 都需排查。

---

## 二、手工功能验收

### 0. 准备测试环境（5 分钟）

用**本地裸仓库模拟 GitHub 远程**，无需联网、无需账号即可完整测试推送/拉取：

```bash
# 1. 创建裸仓库（模拟远程服务器）
git init --bare E:\gitmanager\test_remote.git

# 2. 把裸仓库默认分支指向 main（否则克隆时提示 "remote HEAD refers to nonexistent ref"）
git -C E:\gitmanager\test_remote.git symbolic-ref HEAD refs/heads/main

# 3. 打开软件，地址栏输入 E:\gitmanager\demo_repo 回车（演示仓库已就绪）
# 4. 给演示仓库关联"远程"
git -C E:\gitmanager\demo_repo remote add origin E:\gitmanager\test_remote.git
```

### 1. 基本启动与界面

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1.1 | 双击 `启动Git管理器.bat` 或 `python git_manager.py` | 窗口正常打开，中文无乱码 |
| 1.2 | 观察布局 | 顶部地址栏、快捷按钮行、左侧文件树、右侧四个页签、底部控制台、状态栏齐全 |
| 1.3 | 关闭后重新打开 | 自动恢复上次打开的仓库（或安静地停在初始状态） |

### 2. 打开本地仓库（输入：本地路径）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 2.1 | 地址栏输入 `E:\gitmanager\demo_repo`，回车 | 地址栏回显路径，控制台显示 `$ 打开仓库 …` |
| 2.2 | 查看左侧文件树 | 显示 `src`、`README.md`、`notes.txt`，`src` 为橙色（含修改） |
| 2.3 | 查看「变更」页签 | 两条记录：`src/app.py 已修改`（橙）、`notes.txt 未跟踪`（蓝） |
| 2.4 | 查看「提交历史」页签 | main 分支 2 条提交；「分支」页签 2 个分支（main 带 ●） |
| 2.5 | 状态栏 | 显示当前分支 `main`、变更数量、远程地址 |

### 3. 克隆 GitHub 仓库（输入：GitHub 网址）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 3.1 | 地址栏粘贴 `https://github.com/<用户名>/<仓库名>`，回车 | 弹出"选择克隆保存位置"对话框 |
| 3.2 | 选一个文件夹 | 自动创建 `<仓库名>` 子目录并克隆，完成后自动打开该仓库 |

### 4. 常用操作（状态 / 添加 / 提交 / 推送 / 拉取）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 4.1 | 点「状态」 | 控制台输出 `git status -uall` 完整结果 |
| 4.2 | 点「添加全部」 | 控制台显示 `git add -A` 成功；「变更」页签状态变为已暂存 |
| 4.3 | 点「提交…」，填"测试提交"，点提交 | 提交成功；「提交历史」新增一条记录 |
| 4.4 | 修改一个文件 → 点「提交…」→ 勾选"提交并推送到远程" | 控制台依次输出 add / commit / push 结果 |
| 4.5 | 验证推送（见 4.6） | 在 `test_remote.git` 里能看到提交：`git -C E:\gitmanager\test_remote.git log --oneline` |
| 4.6 | 测试拉取：**从裸远程克隆**第二份仓库 → 在其中提交并推送 → 回原仓库点「拉取」 | 拉取成功，原仓库历史出现新提交 |

> ⚠ 4.6 的第二个克隆**必须从裸仓库** `test_remote.git` 克隆，不要从 `demo_repo` 克隆：
> git 默认禁止推送到一个"已检出分支"的非裸仓库（`refusing to update checked out branch`）。
| 4.7 | 点「查看差异」（先在变更页签选中文件） | 「差异」页签显示 diff，+行绿色 / -行红色 |

### 5. 版本回溯（核心验收项，三种方式都测）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 5.1 | 「提交历史」选中最早一条 → 点「版本回溯」 | 弹出三选一对话框 |
| 5.2 | 选「检出到新分支」→ 执行 | 文件内容回到旧版本，状态栏分支变为 `rollback-<短哈希>`，原提交**不丢失** |
| 5.3 | 切回 main → 再次选旧版本 → 选「软回溯」 | HEAD 回退，之后的提交进入暂存区（变更页签可见），工作区文件保留 |
| 5.4 | 再次选旧版本 → 选「硬回溯」 | 出现红色警告 + 二次确认；确认后该版本之后的提交**永久删除**，文件回到旧版 |
| 5.5 | 验证 | `git -C demo_repo log --oneline` 与界面「提交历史」一致 |

### 6. 分支与暂存

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 6.1 | 「分支」页签选中 `feature-login` → 「切换分支」 | 切换成功，状态栏分支更新，文件树变为该分支内容 |
| 6.2 | 制造改动 → 点「暂存」 | 工作区变干净，改动被保存 |
| 6.3 | 点「恢复暂存」 | 改动原样恢复 |
| 6.4 | 「变更」页签选中文件 → 「放弃所选修改」（有确认） | 该文件未提交改动被丢弃 |

### 7. Token 设置（GitHub 真实推送前）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 7.1 | 点「Token 设置」，粘贴 GitHub 个人访问令牌（勾选 repo 权限） | 提示"Token 已保存"，文件写入 `~/.gitgui_config.json` |
| 7.2 | 再次点「提交并推送」 | 推送到真实 GitHub 仓库成功 |
| 7.3 | 清空 Token 点保存 | 提示"已清除 Token"，后续推送回到认证失败提示 |

---

## 三、异常场景测试（可靠性）

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| E.1 | 地址栏输入不存在的路径 | 弹窗"目录不存在" |
| E.2 | 打开一个普通文件夹（非 git 仓库） | 提示"还不是 git 仓库"，询问是否初始化；点「初始化仓库」后变为可用仓库 |
| E.3 | 未打开任何仓库时点任意操作按钮 | 提示"请先在地址栏打开或克隆一个仓库" |
| E.4 | 无远程仓库时点「推送」 | 控制台报"没有配置远程仓库，无法推送" |
| E.5 | 配置了 https 远程但无 Token 时推送 | 报认证失败，并提示去「Token 设置」 |
| E.6 | 操作进行中（按钮变灰）连点其他按钮 | 按钮被禁用，不会并发执行 git 命令 |
| E.7 | 删除 `git_manager.py` 同目录权限后启动（或设置 `GITGUI_CONFIG` 指向只读目录） | 启动不崩溃，控制台提示"配置写入失败"，功能仍可用 |
| E.8 | 硬回溯前不确认 | 点"取消"不执行任何操作 |

---

## 四、快速回归脚本（可选）

把下面的命令存成 `regression.bat`，每次改完代码一键回归：

```bat
@echo off
cd /d "%~dp0"
python git_manager.py --selftest || (echo 自测失败 & pause & exit /b 1)
echo 自测通过，下面检查 demo_repo 结构...
git -C demo_repo status --short
echo 回归完成
pause
```
